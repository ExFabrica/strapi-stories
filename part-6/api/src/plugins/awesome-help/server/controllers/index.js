'use strict';

const helpController = require('./help');
const settingsController = require('./setting');
const parserController = require('./parser');

module.exports = {
  helpController,
  settingsController,
  parserController
};
